"use client";
import {useState} from 'react';
import {serviceCatalog} from './content';
export default function ContactForm({service='',career=false}:{service?:string;career?:boolean}) {const [reviewed,setReviewed]=useState(false);return <form className="interior-form" onSubmit={e=>{e.preventDefault();setReviewed(true);}}><h2>{career?'Introduce yourself':'Tell us how we can help.'}</h2><div className="form-pair"><label>First name<input required autoComplete="given-name" name="firstName"/></label><label>Last name<input required autoComplete="family-name" name="lastName"/></label></div><div className="form-pair"><label>Email<input required type="email" autoComplete="email" name="email"/></label><label>Phone<input type="tel" autoComplete="tel" name="phone"/></label></div>{career?<label>Area of interest<select name="interest"><option>Automotive technician</option><option>Service advisor</option><option>Other opportunities</option></select></label>:<><label>Vehicle<input placeholder="Year, make and model" name="vehicle"/></label><label>Service<select defaultValue={service} name="service"><option value="">Help me choose</option>{serviceCatalog.map(s=><option value={s.slug} key={s.slug}>{s.name}</option>)}</select></label></>}<label>{career?'Your experience':'What are you noticing?'}<textarea name="message" required rows={4}/></label><button className="shop-primary" type="submit">{career?'Preview inquiry':'Preview service request'}</button><p className="form-caption">Local design preview. This form does not send information.</p>{reviewed&&<div role="status" className="form-result"><b>Your {career?'inquiry':'request'} is ready to review.</b><p>Nothing has been sent. Contact Carroll’s directly at <a href="tel:+12538632524">(253) 863-2524</a> or <a href="mailto:carrolls_garage@yahoo.com">email the shop</a>.</p></div>}</form>;}
